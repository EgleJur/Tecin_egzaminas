CREATE TABLE IF NOT EXISTS WORKER_RATING (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY,
    worker_id BIGINT NOT NULL,
    rating INT NOT NULL,

    CONSTRAINT WORKER_RATING_PK PRIMARY KEY (id),
    CONSTRAINT WORKER_RATING_WORKER_FK FOREIGN KEY (worker_id) REFERENCES WORKER (id)
);
