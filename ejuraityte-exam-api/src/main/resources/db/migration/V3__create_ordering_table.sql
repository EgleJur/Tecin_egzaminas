CREATE TABLE IF NOT EXISTS ORDERING (
    ORDERING_ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
    NAME VARCHAR(255) NOT NULL,
    CLIENT_NAME VARCHAR(255) NOT NULL,
    CONFIRMED BOOLEAN,

     CONSTRAINT ORDERING_PK PRIMARY KEY (ORDERING_ID)
);
